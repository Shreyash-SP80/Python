# Used to combine conditional statements:

a = True
b = False

print(a and b)  # AND: False (both must be True)
print(a or b)   # OR: True (at least one must be True)
print(not a)    # NOT: False (reverses the boolean)